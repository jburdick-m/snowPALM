# GDAL via pip needs the system libgdal-dev installed first, and the
# pip GDAL version must match the system libgdal version exactly.
# pyproj also depends on system PROJ. If install fails, use environment.yml
# with conda instead — it handles the C-library side cleanly.
numpy
scipy
netCDF4
GDAL
pyproj
Pillow
python-dateutil
